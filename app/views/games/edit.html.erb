<div id='pad'>
  <h1>Edit Game</h1>

  <%= render 'form' %>
  <%= button_to 'Delete Game', game_path(@game), method: :delete, data: { confirm: "Are you sure? All reviews for this game will be deleted. This action cannot be undone." } %>
  <%= link_to 'Cancel', game_path %>
</div>
